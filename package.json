{
  "name": "flixlist",
  "private": true,
  "description": "Spotify-for-TV-moments — create and share Netflix playlists",
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "preview": "cd web && npm run preview"
  }
}
